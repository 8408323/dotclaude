{
  "name": "Blocks a commit where an embedded expansion word-splits into a flag",
  "stdin": { "tool_input": { "command": "NV=' --no-verify'; git commit x$NV -m msg" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["shell-expanded"]
}
