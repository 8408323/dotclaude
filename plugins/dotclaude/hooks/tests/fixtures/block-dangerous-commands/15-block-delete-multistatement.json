{
  "name": "Blocks a WHERE-less DELETE even when a later statement has its own WHERE",
  "stdin": { "tool_input": { "command": "psql <<EOF\nDELETE FROM users;\nSELECT * FROM logs WHERE id=1;\nEOF" } },
  "expect_exit": 2
}
