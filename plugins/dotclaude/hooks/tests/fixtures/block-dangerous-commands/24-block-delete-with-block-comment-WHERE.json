{
  "name": "Blocks a WHERE-less DELETE whose only WHERE sits in a block comment",
  "stdin": { "tool_input": { "command": "psql -c \"DELETE FROM users /* WHERE id = 1 */\"" } },
  "expect_exit": 2
}
