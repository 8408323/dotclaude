{
  "name": "Allows a commit whose message has an escaped quote followed by --no-verify as text",
  "stdin": { "tool_input": { "command": "git commit -m \"fix \\\" --no-verify in msg\"" } },
  "expect_exit": 0
}
