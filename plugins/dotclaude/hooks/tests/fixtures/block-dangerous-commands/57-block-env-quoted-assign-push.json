{
  "name": "Blocks push to main via env with a quoted assignment",
  "stdin": { "tool_input": { "command": "env FOO=\"a b\" git push origin main" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["main"]
}
