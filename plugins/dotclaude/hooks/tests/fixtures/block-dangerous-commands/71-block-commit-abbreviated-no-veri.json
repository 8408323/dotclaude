{
  "name": "Blocks a commit using the abbreviated --no-veri form of --no-verify",
  "stdin": { "tool_input": { "command": "git commit --no-veri -m \"x\"" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["--no-v"]
}
