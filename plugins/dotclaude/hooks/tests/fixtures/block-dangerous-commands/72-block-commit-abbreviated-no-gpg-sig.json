{
  "name": "Blocks a commit using the abbreviated --no-gpg-sig form of --no-gpg-sign",
  "stdin": { "tool_input": { "command": "git commit --no-gpg-sig -m \"x\"" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["--no-g"]
}
