{
  "name": "Blocks push to main with a git -c global option before the subcommand",
  "stdin": { "tool_input": { "command": "git -c core.askPass=echo push origin main" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["main"]
}
