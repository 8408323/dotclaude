{
  "name": "Blocks git push --all, which pushes every branch including protected ones",
  "stdin": { "tool_input": { "command": "git push --all origin" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["--all"]
}
