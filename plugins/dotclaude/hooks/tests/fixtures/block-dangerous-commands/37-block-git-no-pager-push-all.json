{
  "name": "Blocks push --all with a --no-pager global option before the subcommand",
  "stdin": { "tool_input": { "command": "git --no-pager push --all" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["--all"]
}
