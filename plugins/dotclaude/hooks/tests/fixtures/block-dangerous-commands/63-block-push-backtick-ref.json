{
  "name": "Blocks push whose refspec comes from a backtick substitution",
  "stdin": { "tool_input": { "command": "git push origin `echo main`" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["shell-expanded refspec"]
}
