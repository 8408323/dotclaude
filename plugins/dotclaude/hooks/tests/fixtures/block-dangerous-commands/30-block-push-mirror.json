{
  "name": "Blocks git push --mirror, which mirrors every ref to the remote",
  "stdin": { "tool_input": { "command": "git push --mirror origin" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["--mirror"]
}
