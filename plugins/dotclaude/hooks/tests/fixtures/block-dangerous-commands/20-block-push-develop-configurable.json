{
  "name": "Blocks push to develop when it is in the configured protected branches",
  "stdin": { "tool_input": { "command": "git push origin develop" } },
  "env": { "CLAUDE_PROTECTED_BRANCHES": "main,master,develop,release" },
  "expect_exit": 2
}
