---
alwaysApply: true
---
<!-- dotclaude:managed — regenerated by /dotclaude:init; customise in the dotclaude repo, not here. To take local ownership, delete this line. -->

# Code Quality

## Anti-defaults (counter common Claude tendencies)

- No premature abstractions. Three similar lines beats a helper used once.
- Don't add features or improvements beyond what was asked.
- Don't refactor adjacent code while fixing a bug.
- No dead code or commented-out blocks. Git has history.
- WHY comments, never WHAT. If code needs a "what" comment, rename instead.
- API docs at module boundaries only, not every internal function.

## Naming

Match the convention of the surrounding code rather than imposing a universal rule.

- **Booleans**: `is` / `has` / `should` / `can` prefix. **Functions**: verb-first.
- **Factories**: `create*` / `make_*`. **Converters**: `to*`. **Predicates**: `is*`.
- Abbreviations only when universally known (`id`, `url`, `api`, `db`, `auth`).
- Wire-format / external identifiers (protobuf fields, API JSON keys, DB columns) keep their on-the-wire spelling verbatim — don't rename to match local style.

## Code Markers

`TODO(author): desc (#issue)` for planned work. `FIXME(author): desc (#issue)` for known bugs. `HACK(author): desc (#issue)` for ugly workarounds (explain the proper fix). `NOTE: desc` for non-obvious context. Owner and issue link required. Never `XXX`, `TEMP`, `REMOVEME`.

## File Organization

- Imports grouped (stdlib, third-party, internal, relative) with a blank line between groups; follow the project's formatter.
- One public class/component per file unless tightly related.
- Function order: public API first, then helpers in call order.
